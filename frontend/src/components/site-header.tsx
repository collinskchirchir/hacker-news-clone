const Header = () => {
  return (
    <header
      className="sticky top-0 z-5- w-full border-border/40 bg-primary/95 backdrop-blur supports-[backdrop-filter]:bg-primary/90">
      Hello
    </header>
  );
};

export default Header;